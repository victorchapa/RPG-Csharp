@model IEnumerable<MVCExample.ModelsDTO.MonsterDTO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Move)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Health)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Defense)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Attack)
        </th>
        <th>
            Weapon Dropped
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Move)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Health)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Defense)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Attack)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Weapon.Name)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.MonsterId }) |
        @Html.ActionLink("Details", "Details", new { id = item.MonsterId }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.MonsterId })
    </td>
</tr>
}

</table>
